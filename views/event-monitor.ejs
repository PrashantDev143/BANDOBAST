<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - E-BANDOBAST</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/event-monitor.css">
    
    <!-- PWA Meta -->
    <meta name="theme-color" content="#1E3A8A">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/dashboard">
                <i class="bi bi-arrow-left me-2"></i>
                Back to Dashboard
            </a>
            
            <div class="navbar-text">
                <i class="bi bi-broadcast me-2"></i>
                Live Event Monitoring
            </div>
        </div>
    </nav>

    <!-- Event Header -->
    <div class="container-fluid mt-3 mb-4">
        <div class="card event-header">
            <div class="card-body">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h4 class="mb-1" id="eventTitle">Loading Event...</h4>
                        <div class="event-meta">
                            <span class="badge bg-primary me-2" id="eventStatus">Active</span>
                            <span class="text-muted">
                                <i class="bi bi-calendar me-1"></i>
                                <span id="eventDateTime">Date & Time</span>
                            </span>
                            <span class="text-muted ms-3">
                                <i class="bi bi-geo-alt me-1"></i>
                                <span id="eventLocationName">Location</span>
                            </span>
                        </div>
                    </div>
                    <div class="col-md-4 text-md-end">
                        <button class="btn btn-danger me-2" id="endEventBtn">
                            <i class="bi bi-stop-circle me-2"></i>End Event
                        </button>
                        <button class="btn btn-outline-primary" id="generateReportBtn">
                            <i class="bi bi-file-earmark-text me-2"></i>Generate Report
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row">
            <!-- Officers List -->
            <div class="col-lg-4 mb-4">
                <div class="card officers-panel">
                    <div class="card-header">
                        <h6 class="mb-0">
                            <i class="bi bi-people me-2"></i>
                            Officers (<span id="officerCount">0</span>)
                        </h6>
                    </div>
                    <div class="card-body p-0">
                        <div class="officers-list" id="officersList">
                            <!-- Officers will be populated here -->
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Live Map -->
            <div class="col-lg-8 mb-4">
                <div class="card map-container">
                    <div class="card-header">
                        <div class="d-flex justify-content-between align-items-center">
                            <h6 class="mb-0">
                                <i class="bi bi-map me-2"></i>
                                Live Location Map
                            </h6>
                            <div>
                                <button class="btn btn-sm btn-outline-primary me-2" id="centerMapBtn">
                                    <i class="bi bi-crosshair me-1"></i>Center View
                                </button>
                                <div class="btn-group" role="group">
                                    <input type="radio" class="btn-check" name="mapView" id="satellite" checked>
                                    <label class="btn btn-outline-primary btn-sm" for="satellite">Satellite</label>
                                    
                                    <input type="radio" class="btn-check" name="mapView" id="street">
                                    <label class="btn btn-outline-primary btn-sm" for="street">Street</label>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="card-body p-0">
                        <div id="map" class="event-map">
                            <!-- Mock map placeholder -->
                            <div class="map-placeholder">
                                <i class="bi bi-geo-alt text-muted"></i>
                                <p class="text-muted">Live map will display here</p>
                                <p class="text-muted small">Real-time officer locations and geofence boundaries</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Alerts & Activity -->
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <div class="d-flex justify-content-between align-items-center">
                            <h6 class="mb-0">
                                <i class="bi bi-bell me-2"></i>
                                Live Activity & Alerts
                            </h6>
                            <button class="btn btn-sm btn-outline-secondary" id="clearAlertsBtn">
                                <i class="bi bi-trash me-1"></i>Clear
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div id="activityFeed" class="activity-feed">
                            <div class="text-center text-muted py-3">
                                <i class="bi bi-broadcast"></i>
                                <p class="mb-0">Monitoring for live updates...</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- End Event Modal -->
    <div class="modal fade" id="endEventModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">End Event</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to end this event?</p>
                    <div class="alert alert-warning">
                        <i class="bi bi-info-circle me-2"></i>
                        This will automatically check out all active officers and stop location tracking.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="confirmEndEvent">End Event</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <!-- Socket.IO -->
    <script src="https://unpkg.com/socket.io@4.7.4/client-dist/socket.io.js"></script>
    <!-- Supabase JS -->
    <script src="https://unpkg.com/@supabase/supabase-js@2"></script>
    <script>
        // Pass event ID from server
        window.eventId = '<%= eventId %>';
    </script>
    <script src="/js/event-monitor.js"></script>
</body>
</html>